{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for all CombatLedger windows.\n\ncore:default grants core:event:listen / core:event:emit / core:event:unlisten so\nthe Tauri v2 plugin:event|listen IPC calls from @tauri-apps/api/event resolve.\nWithout this file, plugin-namespaced IPC calls block silently in production builds\neven though custom invoke commands (get_config, save_config, …) continue to work.",
  "windows": ["settings", "overlay"],
  "permissions": [
    "core:default",
    "dialog:default",
    "shell:default",
    "global-shortcut:default"
  ]
}
